# 2026-04-15 日志

## 会话启动
- 11:38 新会话，用户提到"钱袋子"（MoneyBag 项目）
- 加载 hermanlei-conventions + unity-knowledge-guardian
- MEMORY.md 从 20260328160644 回退复制

## 11:48 MoneyBag 仓库检查
- clone 了 `https://github.com/lj860117/moneybag.git` 到当前 workspace
- 发现用户在家整理了完整规范文档 `docs/my-conventions-and-skills.md`（348行）
- 最新提交 `862e05d [home] v4.1`：估值口径修正+选股PE修复+AI单股点评+Tab导航+全量数据验证
- biosphere-game 仓库最新提交 `df53ba2`：从 git 追踪中移除 .workbuddy/skills（但清理不完整，大量第三方 Skill 仍在追踪中）
- 用户新增 skills-repo：`git@github.com:lj860117/my-skills.git`（PRIVATE），Skill 实体文件统一管理
- 铁律从 11 条扩展到 20 条，新增 P0 事故档案、WAL 协议、双环境同步规则等

## 11:58 SOUL.md 启动清单 + 铁律合并
- 合并家里（my-conventions-and-skills.md）和公司（SOUL.md）两版启动清单
- 启动清单：4步 → 第零步+5步（新增记忆健康检查、SESSION-STATE恢复、WAL协议、收尾闭环）
- 铁律：11条 → 20条（新增可靠性与纪律#12-#17 + moneybag专属#18-#20）
- 公司独有的 Unity wiki 读取 + MEMORY 回退链保留
- 已告知用户 SOUL.md 已修改

## 12:15 SSH Key 配置 + skills-repo 部署
- 公司电脑没有 SSH key，无法 clone 私有仓库
- 用户手动生成了 ed25519 SSH key（hermanlei@tencent.com）并添加到 GitHub
- 用 SSH 成功 clone `git@github.com:lj860117/my-skills.git` 到 `~/.workbuddy/skills-repo/`
- skills-repo 包含 8 个自建 Skill：hermanlei-conventions、scenecraft-dev、unity-rendering、game-designer-toolkit、game-numeric-design、github、unity-scaffolds、web-game-qa
- Windows 符号链接需要管理员权限，改用 Junction（mklink /J）成功
- 8 个 Skill 全部创建 Junction 指向 skills-repo，验证通过

## 13:07 双 AI 投资分析对比
- 调 8 个 API 获取全量数据（dashboard/factors/holdings/scan/regime/decision-data）
- 发现用户当前**空仓**（股票+基金 holdings 均为空）
- DeepSeek Pipeline（default 管线，8 模块，46.7s）结论：**中性观望**（置信度 50%，模块分歧度 0.67）
- Claude 独立分析结论：**谨慎乐观偏中性**，建议分批建仓到 40%
- 输出对比报告：`双AI投资分析对比报告.md`
- 折中建议：先试探建仓 15-20%（宽基 ETF），回调到 4500-4550 再加仓
- 数据质量问题：5 个数据源不可用（北向/SHIBOR/股息/舆情/ETF流），2 个可能异常（全球PE/解禁数据过旧）

## 13:15 数据源不可用原因深度分析
- 根因：AKShare 爬虫库接口频繁变更 + 异常被 try/except 静默吞掉
- 发现 factor_data.py 缺少 `import json` 的代码 Bug（第 292 行 json.loads 会 NameError）
- 全球 PE 中美都返回 73.99 是因为列名匹配失败后取了 `last.iloc[1]`（垃圾值）
- 解禁数据显示 2022 年是因为没有按日期排序/过滤
- 舆情的 `get_market_news`/`get_policy_news` 在 factor_data.py 中未 import（靠外部调用链注入）
- 修复建议：① SSH 检查 AKShare 版本 ② 加 import json ③ 加数据合理性校验 ④ 解禁加日期排序

## 13:25 数据源批量修复（6/7 完成）
修改了 3 个文件，修复 6 个数据源问题：

### factor_data.py 修复：
1. **import json** — 加回缺失的 json 模块导入（舆情 LLM 解析 JSON 需要）
2. **SHIBOR 参数修正** — `market="上海银行同业拆放利率(Shibor)"` → `"上海银行同业拆借市场"`，`symbol="隔夜"` → `"Shibor人民币"`（按 AKShare 1.18.55 官方文档）
3. **股息率接口修正** — stock_index_pe_lg 没有股息率列，加了 PE 推算降级方案 + stock_a_lg_indicator 二次降级
4. **北向资金适配** — 2024年8月沪深交易所不再披露每日净流入明细（政策变更），加了空值过滤+数据新鲜度检测+notice字段

### global_market.py 修复：
5. **全球PE合理性校验** — 抽取 _extract_pe() 辅助函数，加 3~80 区间校验 + 中美PE值相同检测（避免73.99垃圾值）

### market_factors.py 修复：
6. **解禁数据** — 加日期排序（降序）+ 60天过滤，不再显示2022年旧数据
7. **ETF资金流** — 接口名兼容探测（fund_etf_fund_daily_em / fund_etf_spot_em / fund_etf_fund_info_em），加列名匹配失败日志

### 未修复（需服务器验证）：
- 北向资金**数据源本身断了**（交易所政策变更，非代码问题），需要换港交所数据源或改用成交额
- ETF 资金流的接口名是否在服务器 AKShare 版本中存在，需要 SSH 验证

## 16:05 服务器部署 + ETF 修复
- SSH 连接：密码是 `Lj860117!`（带感叹号），用户名 `ubuntu`
- 部署方式：本地 tar 打包 → paramiko SCP 上传 → 服务器解压 → restart systemd
- AKShare 已升级到 1.18.55（服务器已是最新）
- **ETF 资金流彻底修复**：
  - AKShare 1.18.55 没有 ETF 资金流专用接口（fund_etf_fund_flow_em 不存在）
  - fund_etf_fund_daily_em 只有净值数据，增长率列是 "0.03%" 格式（带百分号）
  - 方案B：用增长率排名替代，去掉百分号后转 float
  - 线上验证：1457 只 ETF，涨幅 TOP5（卫星ETF +4.67%）、跌幅 TOP5（能源ETF -1.26%）
- 三次 commit：`169d54b` → `a327195` → `e9aedf9`
- **服务器 venv 权限问题**：已修复！`sudo chown -R ubuntu:ubuntu /opt/moneybag/venv/`
- `pip install -r requirements.txt` 成功，新装了 websockets/uvloop/pyyaml/python-dotenv/httptools/watchfiles 共 6 个包
- 重启服务后 health check 正常：`active (running)`，2 workers，113MB 内存

## 16:30 AI分析 vs 专业机构对比验证
- 对比了 Claude+DeepSeek 双AI分析 与 6家专业机构（招商证券、易方达基金、华龙证券、国泰海通、中信证券、证券市场周刊）
- **核心发现**：
  - AI市场方向判断 B+，跟机构主流一致（谨慎乐观~中性）
  - AI配置方向 B，宽基ETF/黄金/科技/红利跟机构对齐
  - AI最大盲点：**地缘政治完全缺失**（美以伊冲突、油价100美元/桶、霍尔木兹海峡——这是3月A股暴跌6-7%的元凶）
  - AI优势：实时性（46秒）、量化精度（PE百分位/EV计算）、数据透明度
  - 综合评分 3.0/5，定位"快速量化参考"，不能替代机构研报但提供独特价值
- **改进建议**：P0 接入地缘政治事件数据+原油大宗商品影响评估；P1 修复北向资金/ETF数据+行业轮动模块
- 报告输出：`AI分析vs专业机构对比报告.md`
- 解禁数据返回空是正常的（最近 60 天确实没有大型解禁事件）

## 17:30 V6 设计文档初版完成
- 完成 `moneybag/docs/v6-upgrade-design.md`（~730行），覆盖6个模块的完整设计
- 6个模块：A.地缘政治(P0) B.原油扩展(P0) C.北向/ETF修复(P1) D.行业轮动(P1) E.券商研报(P2) F.情景分析(P2)
- 包含跨模块依赖矩阵、5个使用场景、4阶段实施计划

## 18:30 V6 设计文档 V2 — 三大补充
用户提出3个关键问题，设计文档存在重大缺失，已补充：

### 问题1：全量分析多源对比展示
- **发现的致命问题**：后端 3 个 analyze 端点（/stock-holdings/analyze, /fund-holdings/analyze, /agent/analyze）在前端 app.js 中 **0 调用**！违反铁律 #18
- 新增**模块G**：全量分析多源对比展示
  - POST /api/analysis/compare（多源对比API，同时拉 DeepSeek + Claude + 机构）
  - POST /api/analysis/save-external（保存外部分析文本）
  - 前端 Tab 切换 + 分歧汇总展示
  - Claude 录入方式分 3 阶段（手动粘贴→API调用→WorkBuddy自动推送）
- 新增 Phase 5（1天工时）

### 问题2：铁律融入设计
- 新增**铁律检查点矩阵**：20条铁律在V6每一步的具体绑定
- 重点铁律：#18（后端做了前端必须接）、#3（改完立即验证）、#2（每Phase前checkpoint）
- 新增**实施护栏**（硬性规则清单）

### 问题3：每步验证方案
- 旧版只写"部署+验证"空话，新版每一步都有：验什么/怎么验/通过标准/失败处理
- Phase 1-4 各有验证清单 + 端到端验证脚本（bash）
- 模块G 单独验证清单（5个步骤）
- **更新实施计划**：每个任务行加了"铁律检查"和"验证方法"两列

## 18:41 V6 设计文档 V3 — 前后端对应审查
用户关键问题："后端功能都会有前端展示么？多账号有考虑？简化展示哪些需要哪些不需要？"

### 完整扫描结果
- 两个子代理完整扫描：app.js（2890行/82个API端点）+ main.py（3262行/161个路由）+ routers/（13个端点）
- **铁律#18 违规清单**：
  - 🔴 严重违规 9 个（3个analyze + timing + smart-dca + take-profit + 3个news分析）
  - 🟡 中等违规 9 个（独立端点/ML/batch/agent子端点等）
  - 🟢 低风险 9 个（管理/调试/内部API，不需前端）
  - ⚠️ 前端Bug：删除交易API路径+方法都不匹配（POST vs DELETE）

### Simple/Pro 模式规划
- 首页卡片（地缘/油价/行业/机构）→ Simple+Pro 都显示（Simple精简版）
- 新增 Tab（行业轮动/情景分析/全量对比）→ Pro only
- AI聊天增强 → 全体受益（context注入）
- Simple白名单不需修改，新Tab只加到all数组

### 铁律#19 多账号审查
- 前端 userId 统一 ✅（getProfileId 30+处 + getUserId 18处）
- **1处Gap**：模块F 情景分析 GET /api/scenario/{id} 缺 userId 参数
- 其余模块覆盖正常

### 输出文件
- v6-upgrade-design.md 更新（V3补充章节）
- v6-audit-report.md（独立审查报告，含完整矩阵）

## 18:46 V6 设计文档 V4 — 分析历史系统 + Claude自动录入
用户关键需求："Claude录入要自动的，每次分析都能留下来查看历史，DeepSeek和机构也是"

### 模块G 重写核心改动
1. **统一分析存档系统**：新建 `analysis_history.py`，所有来源（DS/Claude/机构）存入 `data/{userId}/analysis_history/` 统一格式 JSON
2. **DeepSeek 自动存档**：改造现有 3 个 analyze 函数，return 前自动调 `save_analysis()` 存档
3. **Claude 自动录入提前到 Phase 5**：WorkBuddy 分析完自动 POST `/api/analysis/external`，不用手动粘贴
4. **机构共识定时快照**：早盘预热时自动存档当日机构观点
5. **前端新增"分析历史" Tab**：按日期分组、支持来源/类型筛选、列表→详情→对比三级视图
6. **新增 5 个 API 端点**：history / detail / latest / compare / external
7. **90 天自动清理**：存档时自动删除 90 天前的旧记录
8. **市场快照随存**：每条分析记录附带当时的沪指/油价/黄金/恐贪，方便事后复盘

### Phase 5 工时调整
- 原版：1天 / 5个任务
- V4版：2天 / 9个任务（5.1-5.9）
- 总工时从 5 Phase 7-10天 → 7-12天

## 19:07 V4 方案用户确认通过
用户确认 V4 设计文档（含分析历史系统 + Claude自动录入）方案 OK，等待用户启动编码阶段。

## 19:10 V4-Final 完整版设计文档整合
用户要求整合一份最终完整设计方案。

### 输出文件
- `moneybag/docs/v6-upgrade-design-final.md` — 完整版（含19个章节，覆盖所有迭代）

### 整合内容
- V1 六模块初版（A~F）
- V2 模块G + 铁律矩阵 + 验证方案
- V3 前后端审查 + Simple/Pro + 多账号
- V4 分析历史 + Claude自动录入 + 多源存档
- 对比报告结论（3.0/5评分 → 4.2~4.5/5目标）
- 完整实施计划（5 Phase / 8-12天）

## 20:00 代码组织约束 + Git Push
- 用户担心代码堆一块，确认后在设计文档实施护栏中新增 3 条代码组织约束：
  1. 新 API 端点必须放 routers/ 目录，main.py 只加 include_router()
  2. _build_market_context() 只做编排循环，具体逻辑在各模块 enrich(ctx) 里
  3. 每 Phase 结束检查 main.py 行数，增长超 50 行必须拆
- Git push 完成：`37a4b4e` — V6设计方案文档（v6-upgrade-design-final.md + v6-upgrade-design.md）
- 用户准备回家开工
